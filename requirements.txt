# 系统依赖（Streamlit Cloud 支持通过 apt 安装缺失的系统库）
apt-get update && apt-get install -y libglib2.0-0 libsm6 libxext6 libxrender1 libfontconfig1

# 核心框架
streamlit==1.32.2
streamlit-drawable-canvas==0.9.2

# 图像处理库（替换为云端兼容版本）
matplotlib==3.8.4
numpy==1.26.4
Pillow==10.2.0
rembg==2.0.47  # 降级到更稳定的兼容版
opencv-python-headless==4.8.0.76  # 无头版（无GUI依赖，适配云端）
onnxruntime==1.17.3
onnx==1.15.0

# 腾讯云SDK（保留之前的降级版本）
tencentcloud-sdk-python==3.0.800
